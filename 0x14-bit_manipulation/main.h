#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>
#include <string.h>

unsigned int binary_to_uint(const char *b);
void print_binary(unsigned long int n);
int _putchar(char c);

#endif /* the and of the #ifndef MAIN_H*/
